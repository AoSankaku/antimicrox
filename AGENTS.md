# Project Instructions

- After changing code, run the appropriate build and verification commands without asking for separate confirmation.
- Do not create Git commits; the user handles commits and GPG authentication.
